<h1>Edit Your review</h1>

<%= render 'review_form', product: @product, review: @review %>

<%= link_to("Delete", product_review_path(@product, @review), method: :delete, data: {confirm: "Are you sure you want to delete this review?"})%>
